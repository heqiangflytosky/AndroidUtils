<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.hq.androidutils">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.switchview.SwitchActivity"></activity>
        <activity android:name=".views.seekbar.SeekBarActivity" />
        <activity android:name=".views.switcher.SwitcherActivity" />
        <activity android:name=".views.banner.BannerActivity" />
        <activity android:name=".thememode.ThemeModeActivity" />
        <activity android:name=".CommonTestActivity" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

</manifest>